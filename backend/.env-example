# Gemini API Key (Required)
GEMINI_API_KEY=

# SQLite DB is used locally (railgpt.db)
# MySQL configs below are kept for future cloud scaling if needed
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=your_password
DB_NAME=railgpt